name: melofi
description: A sleek, Spotify-style app called Melofi, a modern song editor and music enhancer. The app features a dark, minimalistic UI with neon accents and smooth animations. Users can import songs, trim audio, add beats, auto-tune vocals, and apply filters — all in real time. Includes a waveform visualizer, drag-and-drop editing tools, and AI-powered beat matching. Users can save their mixes as playlists, share them, or export them in high quality.
publish_to: 'none'
environment:
  sdk: ^3.7.2
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  audioplayers: ^6.0.0
  file_picker: ^8.0.0
  permission_handler: ^11.0.0
  share_plus: ^9.0.0
  path_provider: ^2.1.0
  flutter_audio_waveforms: ^1.0.4
  flutter_staggered_animations: ^1.1.1
  flutter_svg: ^2.0.9
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/